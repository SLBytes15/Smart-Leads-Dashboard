services:
  - type: web
    name: smartleads-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: PORT
        value: 9000
      - key: NODE_VERSION
        value: 18.18.0